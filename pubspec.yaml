name: safestrap
description: Safe Roblox Bootstrapper
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_floatwing: ^0.3.1
  url_launcher: ^6.3.1
  shared_preferences: ^2.3.3
  lua_dardo_plus: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
