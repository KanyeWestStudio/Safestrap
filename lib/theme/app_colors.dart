import 'package:flutter/material.dart';

class SafestrapColors {
  // Main window
  static const background = Color(0xCC15191F);

  // Glass surfaces
  static const surface = Color(0x991E242B);
  static const surfaceHover = Color(0x662F363F);

  // Sidebar
  static const sidebar = Color(0x88161B21);

  // Text
  static const text = Color(0xFFF1F3F5);
  static const secondaryText = Color(0xFFAAB1BA);

  // Borders
  static const border = Color(0x18FFFFFF);

  // Accent
  static const accent = Color(0xFF8AAFFF);
}
